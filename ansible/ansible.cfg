# ==============================================================================
# EE8203 Network Automation - Ansible Configuration
# ==============================================================================

[defaults]
# Disable SSH host key checking for GNS3 virtual devices
host_key_checking = False

[persistent_connection]
# Increase timeout limits to prevent SSH disconnects during heavy playbook runs
command_timeout = 120
connect_timeout = 120